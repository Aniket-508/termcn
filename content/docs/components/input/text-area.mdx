---
title: "Text Area"
description: "Multi-line text editor with word wrap"
---

<TerminalPreview defaultThemeKey="default">
  <ComponentPreview name="text-area" />
</TerminalPreview>

## Installation

```bash
npx shadcn@latest add https://termcn.vercel.app/r/text-area.json
```

## Usage

```tsx
import { TextArea } from "@/components/ui/text-area";

export function Example() {
  const demoProps = {
    // See the live preview for a working example.
  };

  return <TextArea {...demoProps} />;
}
```

## Dependencies

### Registry Dependencies

- `use-focus`
- `theme-provider`

### npm Dependencies

- `ink`

## Props

| Prop          | Type                                                                                            | Default     | Description                                       |
| ------------- | ----------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------- |
| `value`       | `string`                                                                                        | `undefined` | -                                                 |
| `onChange`    | `(value: string) =&gt; void`                                                                    | `undefined` | -                                                 |
| `onSubmit`    | `(value: string) =&gt; void`                                                                    | `undefined` | -                                                 |
| `placeholder` | `string`                                                                                        | `""`        | -                                                 |
| `rows`        | `number`                                                                                        | `4`         | -                                                 |
| `label`       | `string`                                                                                        | `undefined` | -                                                 |
| `id`          | `string`                                                                                        | `undefined` | -                                                 |
| `borderStyle` | `\| "single" \| "double" \| "round" \| "bold" \| "singleDouble" \| "doubleSingle" \| "classic"` | `"round"`   | Border style. Default: 'round'                    |
| `paddingX`    | `number`                                                                                        | `1`         | Horizontal padding. Default: 1                    |
| `cursor`      | `string`                                                                                        | `"█"`       | Cursor character shown when focused. Default: '█' |

## Registry Files

- `components/ui/text-area.tsx`
