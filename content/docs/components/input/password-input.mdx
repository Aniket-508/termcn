---
title: "Password Input"
description: "Password input with masked characters and reveal toggle"
---

<TerminalPreview defaultThemeKey="default">
  <ComponentPreview name="password-input" />
</TerminalPreview>

## Installation

```bash
npx shadcn@latest add https://termcn.vercel.app/r/password-input.json
```

## Usage

```tsx
import { PasswordInput } from "@/components/ui/password-input";

export function Example() {
  const demoProps = {
    // See the live preview for a working example.
  };

  return <PasswordInput {...demoProps} />;
}
```

## Dependencies

### Registry Dependencies

- `use-focus`
- `theme-provider`

### npm Dependencies

- `ink`

## Props

| Prop          | Type                                                                                            | Default     | Description                                       |
| ------------- | ----------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------- |
| `value`       | `string`                                                                                        | `undefined` | -                                                 |
| `onChange`    | `(value: string) =&gt; void`                                                                    | `undefined` | -                                                 |
| `onSubmit`    | `(value: string) =&gt; void`                                                                    | `undefined` | -                                                 |
| `placeholder` | `string`                                                                                        | `""`        | -                                                 |
| `mask`        | `string`                                                                                        | `"●"`       | -                                                 |
| `showToggle`  | `boolean`                                                                                       | `false`     | -                                                 |
| `label`       | `string`                                                                                        | `undefined` | -                                                 |
| `id`          | `string`                                                                                        | `undefined` | -                                                 |
| `borderStyle` | `\| "single" \| "double" \| "round" \| "bold" \| "singleDouble" \| "doubleSingle" \| "classic"` | `"round"`   | Border style. Default: 'round'                    |
| `paddingX`    | `number`                                                                                        | `1`         | Horizontal padding. Default: 1                    |
| `width`       | `number`                                                                                        | `undefined` | Width of the input box. Default: undefined (auto) |
| `cursor`      | `string`                                                                                        | `"█"`       | Cursor character shown when focused. Default: '█' |

## Registry Files

- `components/ui/password-input.tsx`
